name: Snyk Containers
on: push
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
    - name: Snyk Containers
      uses: snyk/actions/docker@master
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      with:
        image: javulna.jar
